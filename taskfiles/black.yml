# https://taskfile.dev
version: "3"

tasks:
  format:
    desc: Run code formatter
    cmds:
      - set -x && {{.SET_VENV}} black {{.CODE_DIRS}}

  format-dry:
    desc: Verify code formatting
    cmds:
      - set -x && {{.SET_VENV}} black {{.CODE_DIRS}} --check --diff
